__pycache__/
*.pyc
.DS_Store
.env
.state.json
.venv/

# 本地配置(含真实应用名);用 config.example.yaml 作模板
config.yaml

# 生成的启动器(含本机绝对路径),用 make_app.sh 重新生成
AutoTalk.app/

# py2app 打包产物
build/
dist/

# 设计交接包(参考用,不入库)
design/

# 私密数据:不提交到仓库
last_capture.png
skills/memory/*.md
skills/memory/*.summary.md
